Encapsulates a request as an object, thereby letting users parameterize clients with queues, requests, or operations.

Use case: Input handling, where each key press corresponds to a command that can be executed and even undone.